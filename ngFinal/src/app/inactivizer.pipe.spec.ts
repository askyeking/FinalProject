import { InactivizerPipe } from './inactivizer.pipe';

describe('InactivizerPipe', () => {
  it('create an instance', () => {
    const pipe = new InactivizerPipe();
    expect(pipe).toBeTruthy();
  });
});
